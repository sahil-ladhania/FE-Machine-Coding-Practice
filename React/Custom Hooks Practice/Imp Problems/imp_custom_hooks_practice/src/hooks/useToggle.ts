import { useState } from "react";

// Steps:
// 1. Accept an optional initial boolean value, default it to false
// 2. Create a state variable with that initial value
// 3. Create a function that flips the current state to its opposite
// 4. Return the current state value and the flip function as an array

function useToggle(initialValue: boolean = false) {
  // TODO: implement toggle logic
  // return [value, toggle]
}

export default useToggle;
